[package]
name = "policy-runtime"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
policy-core = { path = "../policy-core" }
serde       = { workspace = true }
serde_json  = { workspace = true }
