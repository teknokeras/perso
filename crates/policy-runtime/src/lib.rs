//! perso — policy-runtime
//! Phase 3a: Glob expander + PolicyMap builder
//! Phase 3b: Condition evaluator + evaluate()
//! Phase 4:  WASM boundary bindings (init / evaluate / alloc / dealloc)

pub mod expander;
pub mod evaluator;
pub mod wasm;
